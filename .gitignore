# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler
vendor/
.bundle/
Gemfile.lock

# Local Docker preview (see reference/blog-publishing.md). The container
# copies Gemfile.preview to /tmp to build, but bundler still drops a lock
# beside the source. Build artefact, never commit it.
Gemfile.preview.lock

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local-only tooling and drafts (not part of the published site)
docker-compose.yml
social-posts/
_drafts/

